// Web stub for @react-native-firebase packages
// These are native modules that don't run in browsers.
// The app's firebase.js already handles this gracefully via try/catch.
module.exports = () => null;
module.exports.default = () => null;
